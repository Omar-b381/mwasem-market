# 🛒 برنامج إدارة فواتير مبيعات "مواسم الخيرات"

برنامج سطح مكتب متكامل وسهل الاستخدام، مصمم بلغة Python لإدارة فواتير المبيعات، حساب الإجماليات تلقائياً، حفظ البيانات بشكل آمن، وتصدير الفواتير بصيغة PDF احترافية جاهزة للطباعة.

## 📑 جدول المحتويات (Table of Contents)
1. [وصف المشروع](#-وصف-المشروع)
2. [المشكلة التي يحلها](#-المشكلة-التي-يحلها)
3. [التفاصيل التقنية](#-التفاصيل-التقنية)
4. [الطبقة السردية (القصة وراء المشروع)](#-الطبقة-السردية)
5. [كيفية التشغيل (للمطورين)](#-كيفية-التشغيل)
6. [الاستخدام المقصود](#-الاستخدام-المقصود)
7. [الحقوق والترخيص](#-الحقوق-والترخيص)

---

## 📝 وصف المشروع
تطبيق "مواسم الخيرات" هو حل برمجي محلي (Desktop Application) يستهدف تبسيط وتسريع عملية البيع وإصدار الفواتير. يتيح البرنامج إدخال بيانات العملاء، اختيار الأصناف بمرونة عالية، استيراد قوائم المنتجات، وتوليد فواتير تحمل الهوية البصرية للشركة (الشعار، السجل التجاري، والبطاقة الضريبية) بضغطة زر.

## 🎯 المشكلة التي يحلها
تعاني العديد من الأنشطة التجارية المتوسطة والصغيرة من الاعتماد على الفواتير الورقية أو برامج الجداول الإلكترونية المعقدة والمشتتة، مما يؤدي إلى:
* بطء في إتمام عمليات البيع.
* أخطاء بشرية في حساب الإجماليات والضرائب.
* فقدان البيانات بسبب غياب الأرشفة السليمة.
**يحل هذا المشروع هذه الفجوة** عبر توفير واجهة عصرية وسريعة لا تتطلب اتصالاً بالإنترنت، وتحتفظ بجميع الحركات المالية في قاعدة بيانات محلية آمنة، مع أتمتة كاملة لعملية الحساب وتصميم الفواتير.

---

## 🛠️ التفاصيل التقنية

### التقنيات المستخدمة ولماذا؟ (Tech Stack & Rationale)
* **Python:** اللغة الأساسية للمشروع، تم اختيارها لمرونتها وسرعة التطوير بها.
* **CustomTkinter:** لبناء واجهة المستخدم الرسومية (GUI). تم تفضيلها على Tkinter التقليدية لتوفير تصميم عصري، مريح للعين (Dark/Light mode)، ويحسن من تجربة المستخدم (UX).
* **SQLite3:** لإدارة قواعد البيانات (`invoices_database.db`). تم اختيارها لكونها خفيفة الوزن، مدمجة، ولا تتطلب إعدادات خادم معقدة (Serverless)، مما يجعلها مثالية لتطبيقات سطح المكتب.
* **Pandas & Openpyxl:** لقراءة البيانات والتعامل مع ملفات Excel. توفر Pandas سرعة وكفاءة عالية في معالجة القوائم الطويلة أثناء الاستيراد.
* **WeasyPrint:** لتحويل كود HTML إلى ملفات PDF. تم اختيارها لقدرتها العالية على التعامل مع التنسيقات المعقدة (CSS) ودعمها الممتاز لتوليد مستندات احترافية وقابلة للطباعة بدقة عالية.

### وصف العملية والقرارات الهندسية (The Process)
1. **تصميم الواجهة (UI/UX):** بدأ المشروع بتصميم واجهة نظيفة باستخدام CustomTkinter تركز على تقليل عدد النقرات المطلوبة لإنشاء فاتورة جديدة.
2. **بناء قاعدة البيانات:** تم تصميم مخطط (Schema) قاعدة البيانات لتسجيل الفواتير (Invoices) وتفاصيل الأصناف (Line Items) بشكل يمنع تكرار البيانات ويسهل استرجاعها.
3. **محرك الـ PDF:** بدلاً من استخدام مكتبات الرسم المباشر (مثل ReportLab) والتي قد تكون معقدة في التنسيق، تم اتخاذ قرار هندسي باستخدام **قوالب HTML/CSS** وتمريرها إلى WeasyPrint، مما سهل جداً التحكم في شكل الفاتورة النهائي وإضافة الشعارات.

---

## 📖 الطبقة السردية

### الدافع (Motivation)
جاء الإلهام لهذا المشروع من الحاجة الحقيقية لتسهيل العمل اليومي لمتاجر بيع المنتجات الغذائية والزراعية (مثل الزيتون والمخللات). كانت العملية اليدوية تستهلك وقتاً طويلاً أمام العميل، وكان هناك احتياج ماس لأداة بسيطة لا تتطلب تدريباً طويلاً للموظفين وتعمل بكفاءة دون الحاجة لاشتراكات سحابية مكلفة.

### التحديات والحلول (Challenges & Solutions)
* **التحدي 1: التعامل مع اللغة العربية في ملفات الـ PDF.** واجهت صعوبة في ضبط اتجاه النص (RTL) وظهور الحروف العربية بشكل متصل وصحيح عند تصدير الفاتورة.
  * **الحل:** تم التغلب على ذلك بدمج خطوط عربية مدعومة داخل قوالب الـ HTML واستخدام WeasyPrint الذي يدعم خصائص الـ CSS المتقدمة المتعلقة باتجاه النص.
* **التحدي 2: إدخال مئات الأصناف يدوياً.**
  * **الحل:** تم بناء ميزة "الاستيراد من Excel" باستخدام Pandas، مما سمح للمستخدم برفع قائمة أسعار كاملة بضغطة واحدة وتحديث قاعدة البيانات فوراً.

### النتائج والتأثير (Outcomes & Impact)
أصبح المنتج النهائي تطبيقاً مستقراً يقلل من وقت إصدار الفاتورة من عدة دقائق إلى ثوانٍ معدودة. كما أضفى لمسة احترافية على شكل الفواتير المقدمة للعملاء، وضمن حفظاً آلياً ودقيقاً لجميع سجلات البيع.

### القيود والتحسينات المستقبلية (Limitations & Future Work)
* **القيود الحالية:** البرنامج يعمل حالياً كنسخة محلية (Local) على جهاز واحد، ولا يدعم المزامنة السحابية بين أجهزة متعددة.
* **التحسينات المستقبلية:**
  * إضافة لوحة تحكم (Dashboard) رسومية لعرض تحليلات المبيعات الشهرية.
  * دعم قراءة الباركود (Barcode Scanner) لتسريع إدخال الأصناف.
  * إضافة نظام لإدارة المخزون وتنبيهات النواقص.

---

## 🚀 كيفية التشغيل (Getting Started)

### متطلبات التشغيل (للمطورين)
إذا كنت ترغب في تشغيل الكود المصدري `main.py` أو التعديل عليه، تأكد من تثبيت Python (الإصدار 3.8 أو أحدث)، ثم قم بتثبيت المكتبات المطلوبة:

```bash
# استنساخ المستودع (إذا كان مرفوعاً على جيت هاب)
git clone [https://github.com/yourusername/mawasm-invoices.git](https://github.com/yourusername/mawasm-invoices.git)
cd mawasm-invoices

# تثبيت المكتبات الاعتمادية
pip install customtkinter pandas openpyxl weasyprint pyinstaller

# تشغيل التطبيق
python main.py
